{
  "name": "blobatar",
  "version": "0.2.0-wuu.8",
  "description": "Deterministic geometric blobatars from any string. No dependencies.",
  "type": "module",
  "//sideEffects": "Not `false`. That would license bundlers to drop `import \"blobatar/motion.css\"` as dead weight, and animated blobatars would silently render static for everyone shipping through a bundler.",
  "sideEffects": ["*.css"],
  "//exports": "Vendored as source: consumers (the desktop renderer, the demo grid) bundle the TypeScript directly, so every entry points at src/. There is no dist and no build step here — the desktop's own bundler is the build.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" },
    "./blob": { "types": "./src/blob.ts", "default": "./src/blob.ts" },
    "./uri": { "types": "./src/uri.ts", "default": "./src/uri.ts" },
    "./expression": { "types": "./src/expression.ts", "default": "./src/expression.ts" },
    "./react": { "types": "./src/react.tsx", "default": "./src/react.tsx" },
    "./motion.css": "./src/motion.css",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "bun test",
    "probe": "bun scripts/probe-compose.ts",
    "probe:surface": "bun scripts/probe-surface.ts",
    "check": "bun test"
  },
  "author": "Alain",
  "license": "MIT",
  "peerDependencies": {
    "react": ">=18"
  },
  "peerDependenciesMeta": {
    "react": { "optional": true }
  }
}
